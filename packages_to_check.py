import math # Operatii matematice
import random # Generare de numere aleatoare
import itertools # Combinatorica, permutari, produs cartezian
import collections # Structuri de date avansate (deque, Counter, OrderedDict)
import operator # Functii operaționale utile pentru sortări avansate
import os # Lucru cu sistemul de fișiere
import numpy as np # Lucru cu liste, tabouri bidimensionale (array-uri)
import pandas as pd # Lucru cu date tabelare (fișiere CSV)
import csv # Citire și scriere fișiere CSV
import json # Lucru cu fișiere JSON
import pickle # Salvare și încărcare de obiecte Python
import glob # Cautare de fisiere în directoare
from typing import List, Tuple, Dict, Union # Tipuri pentru funcții
import matplotlib.pyplot as plt # Vizualizare de date
import sklearn # pentru modele
import re # Procesare regex (curatare text)
import cv2 # OpenCV - citire, prelucrare, afisare imagini
from PIL import Image # Pillow - lucru cu imagini
import skimage # Procesare imagini (filtrare, transformari)
import nltk # Natural Language Toolkit (tokenizare, stemming, stopwords)
import spacy
import seaborn
import scipy
import string
import gensim
import tensorflow
import keras
import jax
import flax
import optax
import torch
import lightning
import torchvision
import xgboost
import lightgbm
import catboost
import tqdm
import tensorboard
